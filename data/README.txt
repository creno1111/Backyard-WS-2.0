Data files for FS partition. 
Once the main firmware has been uploaded and the ESP32 board is running WSv2,
browse to the board IP address (may need to use some sort of IP finder utility).
Make sure upload is set for [FILE] upload, then browse to this data 
folder and upload these files. Then, click on the [<-BACK] button.